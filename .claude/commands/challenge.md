---
description: Adversarial pass before promotion — try to break the bundle
---

Load `.claude/skills/adversarial-attacks/SKILL.md`.

Read the full staging contents for the current idea. Read them as if you are looking for reasons this bundle should not ship.

Select attacks from the loaded skill based on the idea's shape — not every attack applies to every bundle. The skill specifies which attacks fit which exposure types.

Record findings in `CHALLENGE.md`. For each finding: **absorb** (return to Shape/Provision and fix) or **declare-acceptable** (note in `CHALLENGE.md` with reasoning).

Do not promote from challenge. Exit to the user for direction on each finding.
