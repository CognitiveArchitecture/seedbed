---
description: Declare decisions, bound scope, split this-iteration-vs-later
---

Load `.claude/skills/scope-perimeter/SKILL.md`.

Read `INTERVIEW.md` and any existing `DECISIONS.md`.

Produce or extend:
- `DECISIONS.md` — every significant choice with reasoning
- `SPEC.md` — idea's defined shape, including an out-of-scope section as substantive as in-scope
- `STATE.md` — updated phase; exposure read recorded if revised

If Shape surfaces unknowns interrogation didn't reach, do not invent answers. Return to `/interrogate` for targeted follow-up, record new Q+A in `INTERVIEW.md`, then resume shaping.

If Shape surfaces that the seed has reshaped substantially, stop and recommend `/seed` re-calibration before continuing.

At Shape completion, assess whether provisioning is warranted. Load `.claude/skills/provisioning-judgment/SKILL.md` to make the call. Skip provisioning when the idea doesn't need it — skipping is legitimate and named.
